type: matching_method

methods:
  exact:
    label: Exact match
    description: Aligns source and target’s IRIs whenever their respective user selected
      property values are identical.
    see_also:
      - https://lenticularlens.org/matching-method/exact

    type: filter
    threshold_range: "{0, 1}"
    sql_template: "{source} = {target}"
